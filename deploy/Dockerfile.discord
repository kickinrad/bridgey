FROM node:22-slim

WORKDIR /app

# Copy package.json and install deps inside container
COPY plugins/bridgey-discord/package.json /app/
RUN npm install --omit=dev

# Copy build output
COPY plugins/bridgey-discord/dist/ /app/dist/

USER node

CMD ["node", "dist/index.js"]
