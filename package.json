{
  "name": "bridgey-marketplace",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "cd apps/daemon && npm run build && cd ../server && npm run build && cd ../discord-bot && npm run build",
    "install:all": "cd apps/daemon && npm install && cd ../server && npm install && cd ../discord-bot && npm install",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.4",
    "msw": "^2.13.3",
    "tsx": "4.22.4",
    "vitest": "^4.1.4",
    "zod": "4.3.6"
  }
}
