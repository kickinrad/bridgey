{
  "hooks": {
    "Stop": [
      {
        "type": "prompt",
        "prompt": "Before ending: read bridgey-deploy.config.json for connection details. If you made changes to config, skills, CLAUDE.md, or other non-memory files this session, offer to run /sync push to update the remote deployment. If the remote agent may have generated new memory entries, offer /sync pull as well. Ask before syncing — never sync silently."
      }
    ]
  }
}
